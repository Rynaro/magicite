# Machine-readable projection of spec.md (ESL C3 tier_artifacts_present, tier=full).
# Authority: spec.md is normative; this file is a faithful summary and MUST NOT diverge.
# Metadata head conforms to .eidolons/ramza/schemas/spec-profile.v1.json (additionalProperties: true);
# RAMZA ships no dedicated spec.yaml schema, so the ECL spec-profile is used as the canonical head.

# ── spec-profile.v1 required head ──────────────────────────────────────────
eidolon: ramza
version: 1.0.0
kind: spec
status: ready-for-vivi
created_at: "2026-08-14T22:41:43Z"
thread_id: "01a00270-c5bd-7c82-9812-71812368c709"
# evidence_anchors_count: docs/0N citations in spec.md
evidence_anchors_count: 102
target_repos:
  - magicite
stories_count: 8
# validation_gates_count MUST equal len(validation_gates) below
validation_gates_count: 9
# confidence: ramza-score --rubric confidence -> VALIDATE
confidence: 0.8475
decisions_resolved_at: "2026-08-14T22:41:43Z"

# ── ESL binding ────────────────────────────────────────────────────────────
change_id: magicite-v1-implementation
esl_tier: full
maker: vivi
checker: kupo
spec_ref: .spectra/changes/magicite-v1-implementation/spec.md
clarification_budget: none

# ── RAMZA gate record (source of truth: plan-state.json) ───────────────────
ramza:
  planning_tier: full
  rightsize_score: 8
  phase_walk: [RS, S, P, E, C, T, R, T, A, DONE]
  skips: []
  refine_cycles: 1
  critic:
    author: ramza-author
    checker: ramza-critic-in-session
    independence: in-session adversarial pass; no second agent was available to RAMZA
    disclosure: risk R11; Kupo is the independent checker at ESL verify
  scores:
    complexity: {total: 10, of: 12, verdict: human_loop}
    explore_selected: {label: H-C layered core + in-process leased writer, total: 85, verdict: elite}
    refine_cycle_1: {total: 3.6, min: 3, verdict: pass}
    confidence: {total: 84.75, verdict: VALIDATE}
  structural_lint: clean
  ears_lint: {criteria: 33, result: pass}
  declared_scope_globs: 13
  criteria_frozen: true

# ── errata (post-emission revisions; full record in plan-state.json errata[]) ──
# Amendments are recorded, never silent. Nothing here re-opens a frozen artifact.
errata:
  - id: A1-REVISED
    at: "2026-08-14"
    record: .spectra/changes/magicite-v1-implementation/decisions/A1-REVISED.md
    disposition: "within declared variance — recorded as an A1 revision; no formal spec amendment, no re-plan"
    authorizing_verdict: "FORGE (Reasoner) — Magicite MCP SDK framework path (A1 re-adjudication), 2026-08-14, gate PASS, confidence 88%"
    implementing_commit: 2d12134
    touched: [stack.framework, stack.framework_assumption, "risks[R6]", "artifacts[].sha256"]
    spec_md_sha256: {prev: 92372ad65a206ede166a64ae9019347d647f54e5160ed5b8da891d412a75c77b, new: 9fca1c088916cda23de4a815ce335b497c25d58cb7d1f0cc2d7a9350aac7baac}
    envelope_integrity_restamped: true
    unchanged: "acceptance_criteria.sha256 (7bd3d184…, byte-identical, not re-frozen); AC text; validation_gates; invariants; tool_surface; milestones; corpus_contradictions; every ramza.scores value"
    verification: "FORGE VC-1 … VC-14: 13 pass, VC-11 deferred to M7 (no Dockerfile yet) as a recorded open item against VG-9; 84 tests pass"
    human_confirmation: pending
  - id: R1-RESTATED
    at: "2026-08-15"
    record: .spectra/changes/magicite-v1-implementation/decisions/R1-RESTATED.md
    disposition: "restatement of a risk-register mitigation so it matches the shipped artifact — no formal spec amendment, no re-plan, no criteria change, no re-scoring"
    authorizing_verdict: "FORGE (Reasoner) — Magicite Signal Integrity and Identity Model, 2026-08-14, RISK-ASSESSMENT, gate PASS, confidence 86%, requires_checker false; section 5.2 routes the spec.yaml R1 mitigation restatement to SPECTRA/RAMZA as an A1-class revision"
    trigger: "An executed adversarial review drove 253 Tier-1 tags / 200 captures for a single skill against R1's documented cap of 3 dw/skill/session, and reproduced it verbatim after the M4 fixes landed. session_id is caller-supplied and unauthenticated, and omitting it mints a fresh session per call — the honest calling convention defeats the cap. Structurally, no per-subject quota can bind under stdio (one OS principal, free session creation), so the bound moves from the subject to the object: per engram, per wall-clock window."
    implementing_commits: [9044d1f (M3 ladder), 3660c5d (M4 signal hygiene), 15eda70 (M5 data-loss fixes), 9f5deb3 (M6 session_end grace floor), 1ce6f99 (IDG docs correction)]
    touched: ["risks[R1].risk", "risks[R1].mitigation", "risks[R1].revised_by", "artifacts[].sha256"]
    spec_md_sha256: {prev: 9fca1c088916cda23de4a815ce335b497c25d58cb7d1f0cc2d7a9350aac7baac, new: 57148a2de70fa723b65cddbce2b57de98327a7ba44b272415101a9263aa79c17}
    spec_md_locations: "reading-contract errata banner; section 3.3 signal_use step-6 note (comment only — signature, defaults and return shape byte-identical); Story M3 user-story overclaim (struck through, not deleted); section Risks R1 row"
    envelope_integrity_restamped: true
    unchanged: "acceptance_criteria.sha256 (7bd3d184…, byte-identical, not re-frozen; no AC references the cap, R1, or the mitigation wording); AC text; validation_gates; invariants; tool_surface; section 3.3 session-resolution rule; milestones; corpus_contradictions; R1's tag/owner/milestone; every ramza.scores value"
    residuals_open: "cap-burning inside another agent's session; cross-session high-salience -1.0 credit hijack; pre-capture session suppression (bounded at M6 by session_end_tag_grace_s, not eliminated); saturation reachable by a patient caller (deliberate); uncapped co-activation row fan-out (potentiation gated); eph_* row growth (ops)"
    carry_forward: "CF-1 adversarial-noise ablation never shipped; CF-2 four guards each held by exactly one test; CF-3 docs/05 names per_skill_window_cap / signal_window, which do not exist in the tree — the spec deliberately does not copy that phrasing (routed to IDG); CF-4 FORGE's reversal conditions live, M7 privilege-boundary check now due; CF-5 Fix-B server-only session minting still an open RAMZA decision"
    verification: "executed post-fix re-test against pinned commit 3660c5d: 253/200 reproduce exactly; 200 captured Tier-1 tags move S by 0.000000000; 4200 captured edge tags commit 0 edges; 100 planted Tier-2 eph_event rows move S by 0.0; R self-reverts to baseline after a 30d anchor; mutation spot-check 8/8 guards detected"
    verified_status_impact: "none — the criteria anchor did not move and no src/, tests/ or docs/ file was touched; see decisions/R1-RESTATED.md section 11"
    human_confirmation: "not required for the restatement; the inherited FORGE ask (confirm M7 packaging does not put the server behind a privilege boundary the client does not share) stands as CF-4"
  - id: DECLARED-EDGES-AMENDED
    at: "2026-08-15"
    record: .spectra/changes/magicite-v1-implementation/decisions/DECLARED-EDGES-AMENDED.md
    disposition: "FORMAL SPEC AMENDMENT - a design gap no code patch can legitimately close (spec.md:593 asked for a value that is unsatisfiable as written), plus two evidence-driven default changes. New criteria in a marked addendum; the frozen criteria file is byte-identical and was NOT re-frozen. No re-plan, no re-scoring, no phase re-walk. UNLIKE the two errata above, this one DOES change executable behaviour."
    authorizing_verdict: "FORGE (Reasoner) - adjudicating the partial falsification of H-BODY, 2026-08-15, composite ROOT-CAUSE + TRADE-OFF + CONSTRAINT-SATISFACTION, standard depth, requires_checker true. Section 4 [VERDICT] 'Damp, don't amputate' (Tier-1 items 1-3); section 5 remedy (a) amend the spec to state where a declared edge's S_edge comes from, (b) write a NEW AC whose GIVEN names the ingestion path, (c) do it WITHOUT editing frozen AC-023."
    trigger: >-
      Confirmed BY EXECUTION (ATLAS): author-declared edges (needs->depends_on, composes, inhibits)
      carry EXACTLY ZERO activation mass, permanently, and no code path can ever raise them.
      route() at spec defaults and route() with every declared type_gain AND inhib_gain zeroed
      return BIT-IDENTICAL scores to 12 decimal places; forcing a declared S_edge=0.9 DOES change
      the ranking, so the path is live and simply never fed. storage/durable.py:203 inserts
      declared edges at storage_strength=0.0 and the ON CONFLICT clause updates only
      dst_id/dangling; core/dream.py:194 + core/signals.py:152-164 restrict potentiation to
      co_activation, the only type ever tagged; core/activation.py:56 then DROPS edges with w<=0,
      so declared edges are ABSENT from the graph rather than weak. Consequences that make this a
      spec problem: plan_confidence (spec.md:593 section 3.3 step 10) is permanently 0.0 for every
      multi-node plan and is client-visible; apply_inhibition is a numeric no-op, so AC-023 is
      unreachable in production (it passes only because tests/unit/core/test_router.py:80 injects
      storage_strength=0.8 by direct SQL); and declared composes/depends_on are silently removed
      from routing.
    ruling: >-
      FORGE's position ADOPTED on substance (declared mutual exclusion and composition are AUTHORED
      ASSERTIONS, not earned Hebbian weights) and SHARPENED on mechanism. (1) The category error was
      the CONFLATION of two semantics in one column, not the 'earned' principle itself: S_eff =
      max(edge.storage_strength, w_authored(edge)) keeps 'weight is earned, not assumed'
      (storage/durable.py:304-309) true of storage_strength, which is the channel it was always
      about. (2) A FLOOR, not FORGE's 'fixed gain independent of S_edge' - learning may exceed an
      assertion, it may not erase one, and S_eff stays in [0,1] so 1 - S*inhib_gain stays positive.
      (3) Scope is all authored types, not just inhibits. FORGE's arithmetic objection to a floor is
      ANSWERED, not accepted: it targets the magnitude 0.1 (~2.7% of graph mass), not the form; at
      the authored magnitude 1.0 the same arithmetic gives ~21.7% of global mass and, since W is
      row-normalised, ~36-53% of a declared node's own outflow. WITHHELD: the hub-penalty structural
      PageRank keeps type_gain-only weighting - it is the one graph mechanism measured to help
      (+0.0286 Hit@1) and re-weighting it is FORGE's own open D3 experiment.
    what_changed: >-
      New normative spec.md section 3.3.1 'Effective edge weight': S_eff = max(storage_strength,
      w_authored), w_authored = declared_edge_strength (new magicite.toml knob, default 1.0) for
      provenance='declared' and 0.0 for learned/derived/distilled. Computed at READ, never stored -
      no column, no second migration, no checkpoint field - so AC-009/AC-010 are untouched (pinned
      by AC-036) and Dream's decay/prune/renormalise still operate on the learned column alone.
      Exactly revertible at declared_edge_strength=0.0 (AC-039). One pure helper
      core/edge_weight.py::effective_strength, guarded by an AST criterion (AC-040). Amended sites:
      section 3.3 step 4 (W_ij = S_eff * type_gain), step 5 (a_i *= 1 - S_eff*inhib_gain, S_eff
      DIRECTLY since type_gain['inhibits']=0.0 by design), step 9 (cycle-break becomes the total
      order (S_eff, dep_name, dependent_name)), step 10 (plan_confidence redefined structurally),
      section 2.6 step 9 (community weights use S_eff; _COMMUNITY_WEIGHT_FLOOR deleted), introspect
      edge rows (additive effective_strength field), eval/bench.py baseline (c) (same helper with
      the learned channel suppressed). Defaults: ppr_restart 0.15 -> 0.85 (MEASURED) and
      w_retrieval 0.15 -> 0.05 with w_similarity 0.30 -> 0.40 (PRECAUTIONARY pending experiment).
      Risks: R10 annotated as FIRED with its measured outcomes; new R12 for the newly-live,
      unmeasured declared-edge mass.
    plan_confidence_fix: >-
      Section 3.3 step 10 WAS mean(S_edge over plan edges) * (resolved_deps/declared_deps), which
      was unsatisfiable: plan edge types are depends_on/composes, always provenance='declared', and
      Dream potentiates only co_activation, so mean(S_edge) was a STRUCTURAL constant. It is now
      |E_sat|/|E| (1.0 when |E|==0) over the emitted plan's declared depends_on/composes edges,
      where E_sat are those that resolve, whose target appears in `order`, and whose ordering
      `order` respects. The S_edge factor is REMOVED rather than floated to 1.0 - a factor that can
      only ever be a constant carries no information in either regime. Dangling targets, depth/size
      truncation and cycle-breaking all fall out of one rule with no extra constants. Deliberate
      behaviour change: the len(order)<=1 -> 1.0 short-circuit is gone, so a lone winner with two
      unresolvable needs now honestly reports 0.0. Pinned by AC-037/AC-038.
    defaults_changed:
      - "ppr_restart 0.15 -> 0.85 (MEASURED, 70 engrams / 210 pre-registered queries, one Config field at a time): Hit@1 0.4619 -> 0.5476 (== embedding baseline (b)), Hit@3 0.7000 -> 0.7476 (> (b)'s 0.7429), MRR 0.5913 -> 0.6398. At 0.15, 85% of activation mass diffused along the 350 derived similar_to kNN edges. NOT w_activation=0 despite its 0.0048 better Hit@1: inhibition acts ONLY on the activation vector, so zeroing that weight makes AC-023 arithmetically UNPROVABLE - breaking a frozen criterion to buy one query in 210 - and turns 45% of the score into a constant zero, taking the hub penalty and community rerank with it."
      - "w_retrieval 0.15 -> 0.05 and w_similarity 0.30 -> 0.40, PRECAUTIONARY PENDING FURTHER EXPERIMENT, explicitly not a measured optimum. Evidence-balance asymmetry, not a new magic number: 0.15 has one strong measurement against it (oracle teacher, MATCHED train/test distributions: held-out Hit@1 0.4697 -> 0.1061; train split 0.4583 -> 0.2847; target-only variant still 0.1818; w_retrieval*R spread = 63% of the query-conditioned signal's amplitude) and zero measurements ever for it. Honest limit carried: the workload is uniform, which makes a popularity prior maximally uninformative. w_activation and w_excitability unchanged; the four still sum to 1.00."
    new_criteria: "AC-034 ... AC-042 in acceptance-criteria-addendum.md (9 criteria, ramza-ears-lint 9/9 pass). AC-034's GIVEN names the register() ingestion path, so it exercises a state production can actually reach. NOT frozen and NOT merged into acceptance-criteria.md: ramza-freeze writes plan-state.json criteria_sha256, which must keep naming the frozen 33."
    ac_023: "PROVENANCE-UNDERSPECIFIED IN ITS GIVEN, and deliberately NOT edited. It says nothing about provenance, so tests/unit/core/test_router.py:80 (direct-SQL storage_strength=0.8) proves it exactly as written - a coverage defect in the frozen criteria, not a checker failure and not a test-fidelity failure. Remedy is AC-034, not a rewrite: editing a frozen criterion to repair its own weakness would destroy the tamper-evidence anchor (R11)."
    spec_md_sha256: {prev: 57148a2de70fa723b65cddbce2b57de98327a7ba44b272415101a9263aa79c17, new: e9efab604467129246e0b70fb25ed70a576bc4fee5934a243881e2151101dd8b}
    spec_md_locations: "reading-contract errata banner; section 2.2 edge DDL comment; section 2.6 step 4 note; section 3.3 route steps 4/5/6/9/10 (superseded values struck in-comment, never deleted); NEW section 3.3.1 Effective edge weight (incl. the routing-defaults table and the release obligation); section 3.3 introspect note; section 4.3 Dream note; Acceptance Criteria pointer + the AC-034...AC-042 addendum block + the AC-023 coverage note; section Risks R10 (fired) and new R12; Handoff."
    envelope_integrity_restamped: true
    unchanged: "acceptance_criteria.sha256 (7bd3d184..., byte-identical, NOT re-frozen; amendments[] still empty; AC-023 not edited); AC-001...AC-033 text; validation_gates (all nine); invariants; tool_surface (introspect gains one ADDITIVE output field - no tool added, removed, renamed or re-signatured); milestones; corpus_contradictions; wire_declared_edges, the DDL and the single migration; Dream's phases and theta_* constants; the hub-penalty PageRank; w_activation/w_excitability/type_gain/inhib_gain/hub_penalty; every ramza.scores value"
    measurement_obligations: "MO-1 re-run the cold 210-query bench after implementation and PUBLISH the new (b)/(c)/(d) numbers (ppr_restart=0.85 was measured on a graph in which declared edges were still inert); MO-2 baseline (c)'s published numbers move and must be re-published or marked superseded; MO-3 report the inhibition delta SEPARATELY - 11 declared inhibits relations that never had any effect now scale their targets' activation by 0.3"
    carry_forward: "CF-1 FORGE section 6's docs/01 + docs/07 corrections are routed to IDG (docs/ is outside RAMZA's write boundary); CF-2 synapses: has no reader, so learned edge weights do NOT survive a DB rebuild and the next checkpoint erases the file's own record - a separate spec-conformance defect against section 2.6 step 4, NOT fixed here; CF-3 the earnable-declared-edge path stays open (the floor form accommodates it with no further spec change); CF-4 FORGE's D1/D2/D3; CF-5 the uncalibrated combination rule (four raw, un-normalised, differently-scaled terms at fixed weights); CF-6 R1-RESTATED's own CF-4/CF-5 remain open"
    verified_status_impact: "The change was already escalated from verified to in_progress by a drift check before this record. UNLIKE the two prior errata this amendment DOES change executable behaviour, so every VG must be re-run after implementation and AC-034...AC-042 attested alongside AC-001...AC-033. AC-009/AC-010 are specifically protected by the read-time (never persisted) design and pinned by AC-036; AC-023 still passes."
    human_confirmation: "recommended before release on two points: declared_edge_strength=1.0 makes authored structure route for the first time and its scale effect is unmeasured (R12; one revertible config scalar), and w_retrieval 0.15 -> 0.05 is explicitly precautionary pending D1/D2. Neither blocks implementation."
  - id: R12-FIRED
    at: "2026-08-15"
    record: .spectra/changes/magicite-v1-implementation/decisions/R12-FIRED.md
    disposition: "MEASUREMENT RECORD + DEFAULT CONFIRMED, NOT CHANGED. R12's release obligation fired and is discharged for MO-1/MO-2; declared_edge_strength stays at 1.0; ppr_restart=0.85 is CONFIRMED on the new graph shape. No rule change, no default change, no acceptance-criteria change, no config change implied, no re-plan, no re-scoring, no phase re-walk. DECLARED-EDGES-AMENDED is NOT superseded - only its ppr_restart-row figures and baseline (c)'s published numbers are, and that record itself obliged their re-measurement."
    authorizing_trigger: "Self-imposed: the release obligation written into DECLARED-EDGES-AMENDED section 8 (MO-1/MO-2/MO-3) and spec.md section 3.3.1. ppr_restart=0.85 had been measured on the OLD kNN-only graph in which declared edges carried zero mass, and the amendment changed that graph underneath it. No new external verdict was required."
    trigger: >-
      The cold 210-query bench was re-run at implementing commit 2d25abb - 70 engrams, 210
      lexically-independent pre-registered queries, cold registry, real fastembed
      BAAI/bge-small-en-v1.5, the same corpus and queries as the original falsification run, nothing
      re-authored. Hit@1: amended defaults (declared_edge_strength=1.0, ppr_restart=0.85) a 0.4048,
      b 0.5476, c 0.5286, d 0.5190; declared edges OFF (declared_edge_strength=0.0,
      ppr_restart=0.85) a 0.4048, b 0.5476, c 0.5286, d 0.5476; both reverted
      (declared_edge_strength=0.0, ppr_restart=0.15) a 0.4048, b 0.5476, c 0.4333, d 0.4905.
      Pre-amendment reference (old weights too): a 0.4048, b 0.5476, c 0.4571, d 0.4619. All figures
      are integer-consistent at n=210 (0.4048=85, 0.5476=115, 0.5286=111, 0.5190=109, 0.4905=103,
      0.4333=91).
    ruling: >-
      (1) ppr_restart = 0.85 is CONFIRMED on the new graph shape - holding declared_edge_strength=0.0
      and moving only the restart, (c) goes 0.4333 -> 0.5286 and (d) goes 0.4905 -> 0.5476. R12's
      stated worry, that 0.5476 was an artefact of the old kNN-only graph and would not transfer,
      does NOT materialise; that clause of R12 is CLOSED. (2) declared_edge_strength SHIPS AT 1.0,
      confirmed on the evidence rather than merely retained. The re-measurement contains an
      ACCIDENTAL ISOLATION that was not read out and is decisive: baseline (c) carries declared
      composes/depends_on into the diffusion graph at FULL authored mass (eval/bench.py:198-208,
      _GRAPH_EDGE_TYPES weighted by effective_strength_no_learned(..., cfg.declared_edge_strength) *
      type_gain) and carries NO inhibition (inhibits is not in _GRAPH_EDGE_TYPES and
      _baseline_c_rank never calls apply_inhibition), NO community rerank, NO hub penalty, NO R, NO
      excitability - and (c) is 0.5286 in BOTH arms, 111 of 210, IDENTICAL. Putting full declared-edge
      mass into the activation graph changed ZERO of 210 top-1 answers. The -6 in (d) therefore
      cannot be attributed to call-site rows 1 or 3 and must arise in a channel present in (d) and
      absent from (c): INHIBITION (row 2) or the COMMUNITY RE-CLUSTERING (row 3). The run separated
      neither - which is exactly what MO-3 was written to obtain. Lowering declared_edge_strength
      would damp a channel measured INERT in order to treat a channel NEVER ISOLATED.
    symmetry_with_w_retrieval_answered: >-
      The challenge is fair and is answered rather than waved off: w_retrieval moved 0.15 -> 0.05 on
      an evidence-balance asymmetry, and declared_edge_strength=1.0 now has one weak measurement
      against it and none for it. But the principle as DECLARED-EDGES-AMENDED section 7.2 actually
      states it has THREE qualifiers - "the downside it guards against is catastrophic; the upside it
      forfeits is unmeasured; the change is one config line" - and this case fails all three.
      EFFECT SIZE: w_retrieval was a 3.6x collapse (held-out Hit@1 0.4697 -> 0.1061, failing on its
      own training split too at 0.4583 -> 0.2847); this is 6 queries in 210, still far above lexical
      0.4048 and above the as-shipped 0.4619. MECHANISM: w_retrieval's was measured (63% of the
      query-conditioned signal's amplitude from a zero-query-conditioned prior); this one is NOT
      identified - the channel in question measured exactly inert. WHAT REVERSAL FORFEITS:
      w_retrieval at 0.05 forfeits nothing structural; declared_edge_strength at 0.0 forfeits three
      shipped behaviours and one addendum criterion. STATISTICAL STANDING: a net swing of 6 in 210
      has a MAXIMUM ATTAINABLE exact-binomial (McNemar) significance of p = 2*0.5^6 = 0.031, and only
      if all six discordant pairs run one way; with four discordances the other way (n=14) it is
      p = 0.180. The b-d gap this spec previously acted on was 18 queries at p = 0.00053. A 3.6x
      collapse justified a precautionary default change; a six-query delta justifies a published
      measurement, a narrowed risk and a pre-registered reversal condition - not a default change.
    uncalibrated_interaction_found: >-
      inhib_gain = 0.7 PREDATES this amendment (section 11 of DECLARED-EDGES-AMENDED lists it as
      deliberately unchanged) and was set when an inhibits edge's S was expected to be a LEARNED value
      distributed over [0,1]. Call-site row 2 pins S to the TOP of that range for EVERY authored
      inhibits edge and, uniquely among the call sites, uses S_eff directly without type_gain (because
      type_gain['inhibits']=0.0 by design). The shipped multiplier is 1 - 1.0*0.7 = 0.3: a 70% cut of
      the inhibited node's activation from one unweighted line of author YAML. inhib_gain HAS NEVER
      BEEN CALIBRATED FOR S=1.0. It is the most plausible mechanism for a six-query regression and it
      has its own dedicated knob - which is DELIBERATELY NOT TOUCHED here, because moving it on an
      unisolated six-query delta would be the identical methodological error at a different address.
      This is a SECOND INSTANCE of CF-5's failure mode: a constant calibrated against one implicit
      distribution, silently re-pointed at another.
    cost_of_reversal_recorded: >-
      declared_edge_strength = 0.0 AS A SHIPPED DEFAULT (as opposed to as an ablation switch) restores
      three of the four defects section 3.3.1 exists to fix. (1) Inhibition is a numeric no-op again,
      so AC-023 is unreachable in production again and AC-034 holds only because its test pins 1.0
      explicitly (tests/acceptance/test_declared_edge_weight.py:33). (2) AC-035's THEN becomes FALSE AS
      WRITTEN - raw weight = declared_edge_strength * type_gain['depends_on'] = 0.0, build_graph drops
      w<=0, the edge is ABSENT not present, and its proving unit's "assert raw_weight > 0"
      (tests/unit/core/test_edge_weight.py:88) reads cfg.declared_edge_strength and would FAIL at the
      shipped default - so shipping 0.0 is NOT a free config change: it needs an AC-035 restatement
      plus a tests/ change, both re-verification events. (3) Community structure loses declared edges
      outright, which is WORSE than pre-amendment because _COMMUNITY_WEIGHT_FLOOR=0.1 was deleted by
      this amendment - 0.0 is the prior state MINUS the floor, not a clean revert. Only plan_confidence
      survives, and only because section 3.3 step 10 was redefined STRUCTURALLY: core/composition.py
      _closure walks declared edges regardless of S_eff, and S_eff enters only as the cycle-break sort
      key, where the (S_eff, dep_name, dependent_name) total order stays deterministic even when every
      candidate ties at 0.0, so AC-042 holds either way. AC-039 REMAINS SATISFIABLE AT ANY SHIPPED
      DEFAULT: its proving unit sets cfg.declared_edge_strength = 0.0 explicitly
      (tests/unit/core/test_edge_weight.py:120), which is what makes every reversal condition a config
      line rather than a code change.
    negative_product_finding: >-
      RECORDED AT FULL STRENGTH, not softened. FORGE ruled H-BODY-b "falsified as implemented, UNTESTED
      AS DESIGNED" precisely because declared edges carried zero mass; that condition is now discharged.
      The result: at FULL authored mass, declared composes/depends_on in the activation graph changed
      ZERO of 210 top-1 answers in the isolated arm (c), and the full pipeline (d) was WORSE by six
      queries. The best (d) has ever measured - 0.5476, exactly tying dense baseline (b) - is measured
      with declared edges OFF. H-BODY-b therefore remains falsified as implemented and is now
      additionally NOT SUPPORTED AS DESIGNED on the only corpus that exists: zero supporting
      measurements, two non-supporting. What the amendment DID deliver, untouched by this run:
      plan_confidence is a real satisfiable ratio instead of a permanent client-visible 0.0
      (AC-037/AC-038); inhibition is reachable from register() rather than only from direct SQL
      (AC-034); cycle-breaking is deterministic instead of dict-iteration-ordered (AC-042); one
      weighting site instead of three divergent local workarounds (AC-040). None of those four is a
      retrieval-accuracy claim. The amendment fixed a correctness and coherence defect; it has NOT been
      shown to improve routing, and a release note must state those separately and the second without
      hedging.
    was_the_amendment_wrong: >-
      NO - and the falsifiable form of that answer is recorded rather than asserted. plan_confidence was
      unsatisfiable as the spec wrote it; AC-023 was unreachable by any production path; three divergent
      local workarounds patched the same zero. All three are defects independent of any benchmark, and
      the first is fixed independently of declared_edge_strength entirely. What IS now in doubt is
      NARROWER and named exactly: the claim that authored edges belong in the DIFFUSION GRAPH
      (call-site rows 1 and 3) has zero supporting measurements and one measured 0/210. If RC-3 also
      returns null, that specific claim should be RETIRED - mechanism kept, implemented and opt-in,
      declared_edge_strength = 0.0 shipped, AC-035 restated. That is RC-3's decision rule and it does
      NOT require RC-2 to reach significance.
    reversal_conditions_pre_registered:
      - "RC-1 (= MO-3, already owed): declared_edge_strength=1.0 with inhib_gain=0.7 vs inhib_gain=0.0, everything else fixed, same corpus and queries. RULE: if the inhibition arm accounts for the whole -0.0286 or more, the defect is the INHIBITION MAGNITUDE, not the authored channel - re-derive inhib_gain and leave declared_edge_strength at 1.0. If it accounts for none of it, the residual is the community re-clustering and call-site row 3 is what gets revisited. Also confirm (c) responds to the knob at all (see caveat_on_own_evidence)."
      - "RC-2: a SECOND, INDEPENDENTLY-AUTHORED registry (engrams and queries by different authors) plus a PAIRED McNEMAR test on (d) at 1.0 vs 0.0. RULE: if (d) at 1.0 is worse at p < 0.05 paired, declared_edge_strength ships at 0.0 - mechanism stays implemented, verified and opt-in - and AC-035 is restated to name a non-zero strength in its GIVEN. The addendum is not frozen; the frozen 33 are untouched either way."
      - "RC-3 (= CF-7): a COMPOSITIONAL query set - H-COMPOSE is still UNTESTED, zero compositional queries have ever been run, and Plan F1 as implemented cannot serve, being a monotone re-encoding of Hit@1. RULE: if declared mass does not improve a composition-sensitive metric there either, the diffusion channel has NO measured benefit on ANY metric and should ship off by default regardless of RC-1."
      - "RC-4: any registry whose declared relations are KNOWN-GOOD BY CONSTRUCTION. Removes the confound that this corpus's needs/composes/inhibits were authored by the same agent that wrote the queries, so a null here may be measuring poor input rather than the design."
      - "NOT SUFFICIENT, recorded so it is not mistaken for progress: re-running the SAME corpus with more queries. It shrinks the error bar on a measurement whose confound (single authorship of both the relations and the queries) is not statistical."
    corpus_limits_carried: >-
      The 210 queries are SINGLE-TARGET retrieval queries - three per engram, one gold answer each.
      Diffusion along a CORRECT needs/composes edge moves activation from the target toward its
      dependencies, which are by construction NOT the gold answer, so on this metric a correct
      composition edge can only be neutral or harmful. This is a real limit and it is DELIBERATELY NOT
      USED AS A SHIELD: it is converted into RC-3, with a decision rule attached. The single-authorship
      confound is RC-4.
    caveat_on_own_evidence: >-
      Stated against RAMZA's own key argument rather than buried: (c)'s invariance across the two arms
      could alternatively be a HARNESS ARTEFACT if the (c) arm did not pick up the config override.
      _baseline_c_rank reads cfg.declared_edge_strength from the same Config the router reads, so a
      single mutated Config would propagate to both - but if the arms were configured separately this
      is possible. Whoever runs RC-1 must confirm (c) responds to the knob at all before concluding
      anything from its invariance.
    options_rejected:
      - "LOWER TO AN INTERMEDIATE VALUE (0.3, 0.5): no point between the endpoints has been measured and both endpoints have. Interpolating toward the better endpoint without evidence re-introduces the second magnitude knob underneath type_gain that DECLARED-EDGES-AMENDED section 4.3 rejected on exactly these grounds, and damps the INERT channel and the SUSPECT channel together in unknown proportion - a fraction of an unmeasured benefit for a fraction of an unattributed harm."
      - "SHIP THE MECHANISM AT 0.0, OPT-IN: reverts three defects the amendment exists to fix, makes AC-035 false at the shipped default and thus forces a tests/ change, and is justified by a delta whose channel is not isolated and whose significance ceiling is p = 0.031. It also makes the DEFAULT configuration one in which no acceptance criterion exercises the mechanism."
      - "LOWER inhib_gain NOW: right suspect, wrong time - zero direct evidence, because the isolation was never run. It is RC-1."
      - "REVERT THE AMENDMENT ITSELF: rejected; see was_the_amendment_wrong."
    r12_disposition: "FIRED 2026-08-15 (mitigation executed as designed: the obligation forced the re-measurement BEFORE release rather than after, and produced a result its authors did not expect). STAYS OPEN at P1, NARROWED. Before: 'newly-live declared-edge mass' in general; ppr_restart possibly non-transferable; MO-1/MO-2/MO-3 all owed; exit condition 're-run and publish'. After: what is unmeasured is THE INHIBITION MAGNITUDE and THE COMMUNITY RE-CLUSTERING; the ppr_restart clause is CLOSED; MO-1 and MO-2 discharged, MO-3 still owed; the exit condition is four PRE-REGISTERED reversal conditions with decision rules attached. Owner unchanged: vivi+kupo."
    spec_md_sha256: {prev: e9efab604467129246e0b70fb25ed70a576bc4fee5934a243881e2151101dd8b, new: 757313fc568c2b2332d67e255f25b493b7f03c16583fb6a8e1693b4e29200440}
    spec_md_locations: "four located patches: the reading-contract errata banner; section 3.3.1's release-obligation paragraph -> the published measurement table + the decision and its five grounds + the rejected intermediate value + the RC-1...RC-4 table; the section Risks R12 row (fired, narrowed, reversal conditions); the Handoff obligation bullet."
    envelope_integrity_restamped: true
    behaviour_change: "NONE. Unlike DECLARED-EDGES-AMENDED, this errata changes no executable behaviour: no default moves, no formula changes, no call-site changes, no criterion changes, no magicite.toml line changes. The re-verification requirements created by DECLARED-EDGES-AMENDED stand unchanged."
    unchanged: "declared_edge_strength = 1.0 (CONFIRMED, not re-derived); S_eff = max(storage_strength, w_authored), w_authored, all seven call sites, the withheld hub-penalty PageRank and the read-time/never-stored property - DECLARED-EDGES-AMENDED is NOT superseded; inhib_gain=0.7, w_activation, w_similarity, w_retrieval, type_gain, hub_penalty and ppr_restart - no routing constant moves, and ppr_restart is CONFIRMED at its amended value; acceptance_criteria.sha256 (7bd3d184..., byte-identical, NOT re-frozen, amendments[] still empty, criteria_sha256 untouched, AC-023 not edited); acceptance-criteria-addendum.md (28888863..., byte-identical - no criterion added, edited or removed; AC-039 stays satisfiable at any shipped default); the 16-tool surface, INV-1...INV-4, the P0 guards, the Tier A/B/C split, all nine validation_gates, milestones, corpus_contradictions; every ramza.scores value, the critic record and the phase walk - not re-run and not re-scored; magicite.toml, src/, tests/, docs/ and change.json - the default does not move, so no config change is implied."
    measurement_obligations: "MO-1 DISCHARGED (new (b)/(c)/(d) numbers published in spec.md section 3.3.1 and decisions/R12-FIRED.md). MO-2 DISCHARGED (baseline (c)'s figures re-published and superseded). MO-3 NOT DISCHARGED - the inhibition delta was never isolated; it is now RC-1 and it is the load-bearing gap."
    carry_forward: "CF-1 (IDG's docs/01 + docs/07 corrections; they now need one more line - the declared-edge design claim was exercised and returned 0/210); CF-2 (synapses: has no reader; still unfixed, still losing weight every day); CF-3 (the earnable path stays open - S_eff's FLOOR form is what keeps it open); CF-4 (FORGE D1/D2/D3); CF-5 (the uncalibrated combination rule - this record's inhib_gain-at-S=1.0 finding is a SECOND INSTANCE of exactly that failure mode); CF-6 (R1-RESTATED's own residuals). NEW: CF-7 - a compositional query set, owner vivi/atlas; RC-3 depends on it."
    maker_checker_disclosure: "This errata CONFIRMS A DEFAULT RAMZA SET, on evidence that arrived after it was set - a maker evaluating their own work, disclosed rather than laundered, the same posture as R11. Bounded three ways: the decision rules are PRE-REGISTERED (RC-1...RC-4) so the next person applies them without re-litigating the judgement, and RC-3 can retire the diffusion claim WITHOUT requiring RC-2 to reach significance; the negative finding is recorded at full strength, including that the best (d) ever measured is measured with declared edges OFF; and the strongest argument against RAMZA's own key evidence is recorded in caveat_on_own_evidence with an instruction to check it first. Kupo attests RC-1's outcome at ESL verify; the frozen criteria hash, not this record, is the tamper anchor."
    verified_status_impact: "None from this errata on its own - no behaviour changes, no default moves, no criterion changes. The change remains in_progress from the DECLARED-EDGES-AMENDED drift escalation and that record's re-verification requirements stand. What a re-verifier should check independently here is the hash chain, which moved by design: spec.md e9efab60... -> 757313fc..., mirrored in artifacts[], spec.envelope.json (artifact.sha256 / integrity.value / x_ramza_amendment[3]) and plan-state.json errata[3]."
    human_confirmation: "RECOMMENDED BEFORE RELEASE, and sharper than DECLARED-EDGES-AMENDED's was. (1) declared_edge_strength=1.0 is confirmed against a six-query regression whose responsible channel was never isolated; a human may reasonably prefer to hold release until RC-1 (= MO-3, already owed) runs, since it is one cheap arm and it decides which knob is even under discussion. (2) The product claim that authored graph structure improves routing now has ZERO supporting measurements and TWO non-supporting ones, and any release note must say so without hedging - that is a claim-scope decision, not a knob. (3) w_retrieval 0.15 -> 0.05 remains explicitly precautionary pending D1/D2. None of the three blocks implementation."

# ── stack decisions (fixed upstream; A1 reopened once — see errata A1-REVISED) ─
stack:
  language: python
  python_requires: ">=3.11"
  framework: mcp.server.lowlevel.Server (official MCP Python SDK, mcp>=2.0,<3.0)
  framework_assumption: >-
    A1-REVISED — resolved on execution-verified evidence 2026-08-14; blast radius confirmed at
    src/magicite/mcp/app.py (+ pyproject.toml, uv.lock, tests), inside A1's own declared bound.
    Handlers register via the public constructor kwargs on_list_tools=/on_call_tool=; the FastMCP
    private-attribute reach-through and validate_input=False are gone. Supersedes A1 (was:
    mcp.server.fastmcp.FastMCP on mcp>=1.12; standalone fastmcp>=2 would change only
    src/magicite/mcp/*). Record: decisions/A1-REVISED.md; verdict: FORGE 2026-08-14; commit 2d12134.
  transport: stdio
  storage: sqlite (WAL, embedded, single file)
  embeddings:
    default_provider: fastembed (ONNX BAAI/bge-small-en-v1.5, baked into the image)
    alt_providers: [ollama, hashing]
    banned: torch
  distribution:
    primary: "ghcr.io/rynaro/magicite (sibling-MCP pattern: --cap-drop ALL, no-new-privileges, non-root UID 10001, digest-pinned)"
    secondary: pip install magicite (development)
  out_of_scope: [served HTTP/OAuth/Postgres profile, CRDT merge, GNN layer, fine-grained telemetry KPI, in-server generative model, R4/R5 tools]

# ── architecture invariants (the four commitments in spec Approach) ────────
invariants:
  - id: INV-1
    statement: the engine is a library; MCP is an adapter (magicite.core never imports the MCP framework)
  - id: INV-2
    statement: writers are a place, not a rule — durable mutation only inside the leased WriterExecutor
  - id: INV-3
    statement: files are the source of truth (Tier A+B in .egr.md); the graph DB is a rebuildable index
  - id: INV-4
    statement: the 16-tool surface is frozen at M0; unimplemented bodies return typed not_implemented
p0_enforcement:
  g1_authorizer: sqlite3 set_authorizer DENYs INSERT/UPDATE/DELETE on any non-eph_ table for hot-path connections (mechanism verified executable)
  g2_lease: storage/durable.py asserts WriterLease ownership on every public write
  g3_dream_context: writer.write_plasticity/write_synapses assert the Dream ContextVar set only by dream.checkpoint_phase()
  tier_gate: plasticity.apply() raises P0Violation for any Tier-0 signal targeting S

# ── MCP surface (16 tools; count reconciliation in CR-5) ───────────────────
tool_surface:
  count: 16
  tools:
    - {name: route, risk_class: R0, side_effect: none, signal_tier: 0, idempotent: true}
    - {name: load_skill_body, risk_class: R0, side_effect: none, signal_tier: 0, idempotent: true}
    - {name: introspect, risk_class: R0, side_effect: none, signal_tier: null, idempotent: true}
    - {name: flag_dead, risk_class: R0, side_effect: none, signal_tier: null, idempotent: true}
    - {name: signal_use, risk_class: R1, side_effect: ephemeral, signal_tier: 1|2, idempotent: true}
    - {name: signal_outcome, risk_class: R1, side_effect: ephemeral, signal_tier: 1|2, idempotent: true}
    - {name: session_end, risk_class: R1, side_effect: ephemeral+enqueue, signal_tier: 1, idempotent: true}
    - {name: register, risk_class: R2, side_effect: filesystem+durable_index, signal_tier: null, idempotent: true}
    - {name: sync, risk_class: R2, side_effect: durable_index, signal_tier: null, idempotent: true}
    - {name: checkpoint, risk_class: R2, side_effect: filesystem, signal_tier: null, idempotent: true}
    - {name: export, risk_class: R2, side_effect: filesystem, signal_tier: null, idempotent: true}
    - {name: consolidate, risk_class: R3, side_effect: batch, signal_tier: null, idempotent: true}
    - {name: nucleate, risk_class: R3, side_effect: proposal, signal_tier: null, idempotent: true}
    - {name: sharpen, risk_class: R3, side_effect: proposal->durable, signal_tier: null, idempotent: true}
    - {name: promote, risk_class: R3, side_effect: proposal->durable, signal_tier: null, idempotent: true}
    - {name: archive, risk_class: R3, side_effect: proposal->durable, signal_tier: null, idempotent: true}

# ── milestones (ordered; M0 first, M1-M3 not reorderable, M4>M3, M5>M4) ────
milestones:
  - id: M0
    title: "Walking skeleton \u2014 server boots, register -> route -> introspect end-to-end"
    duration_days: 3
    risk_tag: P0
    executor_hint: frontier (explicit file list + schemas)
    proves: [AC-001, AC-002, AC-003, AC-004, AC-005, AC-006]
    file_set: "pyproject.toml, uv.lock, src/magicite/{config,errors}.py, obs/logging.py, storage/db.py, storage/migrations/001_init.sql, engram/{model,parser,ids,lint,writer}.py, engram/schema/engram-0.2.schema.json, embeddings/hashing_provider.py, core/router.py (reduced), mcp/{app,registry,schemas,bind_*}.py, tests/fixtures/toy-registry/"
  - id: M1
    title: "Storage completeness, engram round-trip, and the rebuild invariant"
    duration_days: 4
    risk_tag: P0
    executor_hint: frontier (goals + contracts)
    proves: [AC-007, AC-008, AC-009, AC-010, AC-018, AC-021]
    file_set: "storage/{durable,ephemeral,queries}.py, engram/skillmd.py, engram/lint.py (import profile), core sync() pipeline, export(), tests/acceptance/test_rebuild_invariant.py"
  - id: M2
    title: "Routing engine \u2014 embeddings, activation, communities, composition"
    duration_days: 5
    risk_tag: P1
    executor_hint: frontier (goals + contracts)
    proves: [AC-011, AC-012, AC-022, AC-023]
    file_set: "embeddings/{base,fastembed_provider,ollama_provider,cache}.py, core/{activation,communities,composition,router}.py, mcp/bind_retrieval.py, tests/integration/test_route_latency.py"
  - id: M3
    title: "Signals \u2014 the Tier 0/1/2 ladder, tags, and caps"
    duration_days: 4
    risk_tag: P0
    executor_hint: frontier (goals + contracts)
    proves: [AC-013, AC-014, AC-015, AC-019]
    file_set: "core/{session,signals}.py, mcp/bind_signals.py, obs/events.py, storage/authorizer.py, docs/adapters/claude-code.md, tests/acceptance/test_p0_hot_path.py"
  - id: M4
    title: "Dream worker \u2014 lease, seven phases, synapses: checkpoint"
    duration_days: 5
    risk_tag: P0
    executor_hint: frontier (goals + contracts)
    proves: [AC-016, AC-017, AC-020, AC-021, AC-025, AC-032, AC-033]
    file_set: "storage/lease.py, core/{dream,plasticity,decay,audit}.py, mcp/bind_dream.py, tests/acceptance/test_dream_idempotent.py"
  - id: M5
    title: "Lifecycle, trust, and governance"
    duration_days: 4
    risk_tag: P0
    executor_hint: frontier (goals + contracts)
    proves: [AC-016, AC-024, AC-027, AC-028]
    file_set: "core/{lifecycle,fitness,approvals}.py, mcp/bind_lifecycle.py, .spectra/approvals mirror, docs/operations.md"
  - id: M6
    title: "Distillation proposals, evaluation harness, standing KPIs"
    duration_days: 4
    risk_tag: P2
    executor_hint: frontier (goals + contracts)
    proves: [AC-029, AC-030]
    file_set: "core/distill.py, eval/{bench,metrics,ablations}.py, obs/kpi.py, tests/integration/test_bench.py"
  - id: M7
    title: "Packaging, hardening, host adapter docs"
    duration_days: 2
    risk_tag: P1
    executor_hint: mid (copy atlas-aci Dockerfile shape)
    proves: [AC-026, AC-031]
    file_set: "Dockerfile, Dockerfile.dev, .dockerignore, .github/workflows/{ci,release}.yml, README.md, magicite doctor"
  # total: 31d across 8 packages, each <= 8d

# ── acceptance criteria (authority: acceptance-criteria.md, frozen) ────────
acceptance_criteria:
  count: 33
  source: .spectra/changes/magicite-v1-implementation/acceptance-criteria.md
  sha256: 7bd3d1842de8d0d55c00fc4803e52e012885a26094cc928b7801af556eada448
  frozen_by: ramza-freeze
  # ── addendum (DECLARED-EDGES-AMENDED, 2026-08-15): NINE additional criteria that are
  #    NOT part of the frozen set and are NOT merged into it. ramza-freeze writes
  #    plan-state.json criteria_sha256, and that pointer must keep naming the frozen 33.
  addendum:
    count: 9
    source: .spectra/changes/magicite-v1-implementation/acceptance-criteria-addendum.md
    sha256: 2888886351b9d658ab98bf1d2e94d77e43850201130a15b26be9c63f3e27ef02
    frozen_by: null
    lint: "ramza-ears-lint: 9/9 pass"
    added_by: DECLARED-EDGES-AMENDED
    attested_with: "AC-001 ... AC-033 (Kupo attests both sets)"
    ac_023_note: "AC-023 is provenance-underspecified in its GIVEN - a coverage defect in the frozen set, not a checker failure. It is NOT edited; AC-034 is the criterion whose GIVEN names the register() ingestion path."
    ids:
      - {id: AC-034, form: event-driven, verify: "test: tests/acceptance/test_declared_edge_weight.py::test_inhibition_is_reachable_from_register"}
      - {id: AC-035, form: event-driven, verify: "test: tests/unit/core/test_edge_weight.py::test_declared_edge_enters_the_activation_graph"}
      - {id: AC-036, form: state-driven, verify: "test: tests/unit/storage/test_durable.py::test_authored_weight_is_never_persisted"}
      - {id: AC-037, form: event-driven, verify: "test: tests/integration/test_route_end_to_end.py::test_plan_confidence_is_one_when_fully_resolved"}
      - {id: AC-038, form: event-driven, verify: "test: tests/integration/test_route_end_to_end.py::test_plan_confidence_reports_the_unresolved_share"}
      - {id: AC-039, form: unwanted-behavior, verify: "test: tests/unit/core/test_edge_weight.py::test_zero_declared_strength_is_an_exact_revert"}
      - {id: AC-040, form: ubiquitous, verify: "test: tests/unit/test_p0_enforcement.py::test_edge_weight_helper_is_the_only_weighting_site"}
      - {id: AC-041, form: event-driven, verify: "test: tests/unit/storage/test_queries.py::test_edge_rows_report_effective_strength"}
      - {id: AC-042, form: event-driven, verify: "test: tests/unit/core/test_composition.py::test_cycle_break_is_deterministic"}
  ids:
    - {id: AC-001, form: event-driven, verify: "test: tests/acceptance/test_stdio_handshake.py::test_initialize"}
    - {id: AC-002, form: ubiquitous, verify: "test: tests/acceptance/test_stdio_handshake.py::test_stdout_is_protocol_only"}
    - {id: AC-003, form: event-driven, verify: "test: tests/acceptance/test_tool_manifest.py::test_sixteen_tools"}
    - {id: AC-004, form: ubiquitous, verify: "test: tests/unit/mcp/test_registry.py::test_metadata_complete"}
    - {id: AC-005, form: unwanted-behavior, verify: "test: tests/unit/mcp/test_schemas.py::test_unknown_field_rejected"}
    - {id: AC-006, form: event-driven, verify: "test: tests/acceptance/test_walking_skeleton.py::test_register_route_introspect"}
    - {id: AC-007, form: ubiquitous, verify: "test: tests/unit/engram/test_writer.py::test_atomic_replace_never_partial"}
    - {id: AC-008, form: event-driven, verify: "test: tests/integration/test_register_import.py::test_import_profile_downgrades"}
    - {id: AC-009, form: event-driven, verify: "test: tests/acceptance/test_rebuild_invariant.py::test_durable_state_survives_rebuild"}
    - {id: AC-010, form: state-driven, verify: "test: tests/acceptance/test_rebuild_invariant.py::test_only_tier_c_is_lost"}
    - {id: AC-011, form: event-driven, verify: "test: tests/integration/test_route_latency.py::test_p95_under_100ms"}
    - {id: AC-012, form: event-driven, verify: "test: tests/integration/test_route_end_to_end.py::test_topological_plan_order"}
    - {id: AC-013, form: unwanted-behavior, verify: "test: tests/acceptance/test_p0_hot_path.py::test_authorizer_denies_durable_write"}
    - {id: AC-014, form: unwanted-behavior, verify: "test: tests/unit/core/test_plasticity.py::test_tier0_cannot_move_S"}
    - {id: AC-015, form: event-driven, verify: "test: tests/unit/core/test_signals.py::test_tier_assigned_server_side"}
    - {id: AC-016, form: unwanted-behavior, verify: "test: tests/unit/core/test_lifecycle.py::test_promote_denied_below_evidence_bar"}
    - {id: AC-017, form: event-driven, verify: "test: tests/unit/core/test_plasticity.py::test_metaplastic_saturation_bound"}
    - {id: AC-018, form: event-driven, verify: "test: tests/integration/test_skillmd_roundtrip.py::test_export_import_stable"}
    - {id: AC-019, form: event-driven, verify: "test: tests/unit/mcp/test_idempotency.py::test_replay_returns_cached_response"}
    - {id: AC-020, form: event-driven, verify: "test: tests/acceptance/test_dream_idempotent.py::test_second_run_is_a_noop"}
    - {id: AC-021, form: event-driven, verify: "test: tests/unit/engram/test_writer.py::test_render_is_deterministic"}
    - {id: AC-022, form: state-driven, verify: "test: tests/unit/core/test_communities.py::test_fallback_detector"}
    - {id: AC-023, form: event-driven, verify: "test: tests/unit/core/test_router.py::test_inhibition_lowers_score"}
    - {id: AC-024, form: ubiquitous, verify: "test: tests/unit/test_p0_enforcement.py::test_forbidden_imports"}
    - {id: AC-025, form: unwanted-behavior, verify: "test: tests/integration/test_dream_cycle.py::test_single_writer_enforced"}
    - {id: AC-026, form: event-driven, verify: "test: tests/acceptance/test_docker_smoke.py::test_offline_handshake"}
    - {id: AC-027, form: unwanted-behavior, verify: "test: tests/unit/core/test_approvals.py::test_r3_requires_approval_by_default"}
    - {id: AC-028, form: event-driven, verify: "test: tests/integration/test_register_import.py::test_exec_block_quarantined"}
    - {id: AC-029, form: event-driven, verify: "test: tests/integration/test_bench.py::test_baseline_metrics_emitted"}
    - {id: AC-030, form: event-driven, verify: "test: tests/unit/core/test_audit.py::test_hub_detection"}
    - {id: AC-031, form: ubiquitous, verify: "command: uv pip list --format json | jq -e 'map(.name) | index(\"torch\") == null'"}
    - {id: AC-032, form: event-driven, verify: "test: tests/integration/test_dream_cycle.py::test_session_end_debounce"}
    - {id: AC-033, form: state-driven, verify: "test: tests/integration/test_dream_cycle.py::test_decay_floor_archives_never_deletes"}

# ── validation gates Kupo runs at ESL verify (spec section 7.2) ────────────
validation_gates:
  - {id: VG-1, command: "uv run ruff check . && uv run mypy src", proves: "lint + type"}
  - {id: VG-2, command: "uv run pytest -q --cov=src/magicite --cov-fail-under=70", proves: "unit + integration with coverage floor"}
  - {id: VG-3, command: "uv run pytest -q -m acceptance", proves: "the AC-mapped acceptance suite"}
  - {id: VG-4, command: "uv run magicite tools | jq '.tools | length'  # == 16", proves: "tool-surface count (AC-003)"}
  - {id: VG-5, command: "uv run pytest tests/acceptance/test_stdio_handshake.py", proves: "stdio handshake + stdout purity (AC-001, AC-002)"}
  - {id: VG-6, command: "uv run pytest tests/acceptance/test_rebuild_invariant.py", proves: "rebuildable-index invariant (AC-009, AC-010)"}
  - {id: VG-7, command: "uv run pytest tests/acceptance/test_p0_hot_path.py", proves: "P0 authorizer + AST import guards (AC-013, AC-014, AC-024)"}
  - {id: VG-8, command: "uv run pytest tests/acceptance/test_dream_idempotent.py", proves: "Dream run idempotency + render determinism (AC-020, AC-021)"}
  - {id: VG-9, command: "docker build -t magicite:verify . && uv run pytest tests/acceptance/test_docker_smoke.py", proves: "offline containerised handshake (AC-026)"}

# ── risk register ──────────────────────────────────────────────────────────
risks:
  - id: R1
    tag: P0
    owner: vivi
    milestone: "M3,M4"
    risk: "Signal poisoning / adversarial valence — docs/07's 'bounded, not eliminated'. The risk framing was always right; its mitigation bookkeeping was not (see revised_by)."
    mitigation: >-
      Object-keyed and temporal, not identity-keyed. Under local-first stdio no per-subject quota can
      bind (one OS principal shared by client and server; session_id is caller-supplied, unauthenticated
      and free to mint), so every bound below is keyed on an engram and on elapsed wall clock — what a
      caller cannot mint. Two-phase commit (tags != weights); Tier-0 barred from S; Dream-only S writes
      (G3); eph_tag is the sole plasticity-S input and eph_event never is, now held by a test rather than
      a docstring (100 planted Tier-2 valence=+1.0 rows move S by exactly 0.0); a per-engram refractory
      window on the R bump (eta_r_refractory_s=30s) so R counts occasions, not calls; decay applied at
      read (lambda_R 0.1/day, lambda_S 0.01/day) so influence self-reverses with no Dream run, no sync()
      and no human; spacing-gated potentiation (tau_spacing_hours=6.0) so a burst establishes anchors and
      commits nothing (200 captured Tier-1 tags -> delta S = 0.000000000; the first commit needs >= ~85
      min of elapsed time, then at most one commit per engram per Dream run, ceiling +0.048 per
      occasion); bounded retroactive credit (retroactive_credit_max=10); metaplastic saturation (bounds
      the per-event step, never the number of events); Tier-1 weighted 0.6. per_skill_session_cap=3 is
      RETAINED as runaway protection and is NOT counted as an anti-poisoning control — an executed
      adversarial review drove 253 tags / 200 captures for a single skill against it, on the honest path.
      Residuals, verified post-fix and still open: cap-burning inside another agent's session, and a
      cross-session high-salience -1.0 retroactive-credit hijack; closing either needs caller identity
      (structurally impossible under stdio) or a change to the frozen 16-tool surface. The originally
      cited 'adversarial-noise robustness test in the ablation suite' never shipped (carry-forward CF-1);
      the standing evidence is two executed adversarial reviews plus an 8-guard mutation spot-check.
      Full record, evidence and residual list: decisions/R1-RESTATED.md.
    revised_by: >-
      R1-RESTATED (2026-08-15): the mitigation previously read 'two-phase commit, Tier-0 barred from S,
      Tier-1 capped 3 dw/skill/session at weight 0.6, metaplastic saturation, Dream-only S writes'. The
      capped clause described a control that cannot bind: the cap is keyed on (session_id, engram_id),
      session_id is caller-supplied and unauthenticated, and omitting it mints a fresh session per call,
      so the documented calling convention defeats it with no malice (253 tags / 200 captures, executed).
      Authorizing verdict: FORGE (Reasoner) — Magicite Signal Integrity and Identity Model, 2026-08-14,
      gate PASS, confidence 86%, section 5.2 routing the spec edit to RAMZA. Risk statement substance,
      tag (P0), owner and milestone are unchanged; no AC, VG command, invariant, tool signature,
      milestone or section-9 resolution was touched; nothing was re-scored.
  - id: R2
    tag: P0
    owner: vivi
    milestone: "M3"
    risk: "Tier-2 provenance spoofing"
    mitigation: "tier assigned server-side; Tier 2 requires adapter_token == MAGICITE_HOOK_TOKEN (AC-015)"
  - id: R3
    tag: P1
    owner: vivi
    milestone: "M4"
    risk: "Checkpoint file churn above the 5% target"
    mitigation: "epsilon-hysteresis 0.05, lazy S materialisation, state-derived dirty set, checkpoint_write_ratio CI assertion"
  - id: R4
    tag: P1
    owner: vivi
    milestone: "M2,M7"
    risk: "Embedding provider weight / offline behaviour"
    mitigation: "model baked at build, MAGICITE_EMBEDDING_OFFLINE=1, hashing provider for CI, torch banned (AC-031)"
  - id: R5
    tag: P1
    owner: vivi
    milestone: "M2"
    risk: "Leiden dependency fragility (igraph/leidenalg wheels)"
    mitigation: "CommunityDetector protocol; Leiden as an extra; label-propagation fallback reported by introspect (AC-022)"
  - id: R6
    tag: P2
    owner: vivi
    milestone: "M0"
    risk: "MCP SDK tool-metadata API drift"
    mitigation: >-
      metadata owned by TOOL_REGISTRY; MCP annotations/_meta are projections; magicite tools prints
      the authoritative manifest (AC-004). Handler registration is now a public constructor API
      (Server(name, on_list_tools=..., on_call_tool=...)) and _meta is a first-class mcp.types.Tool
      kwarg, so the residual collapses to ordinary type-import drift.
    revised_by: "A1-REVISED (2026-08-14): retitled from 'FastMCP metadata API drift' and downgraded P1 -> P2 — the original subject, the private-attribute reach-through app._mcp_server.list_tools()(...) / .call_tool(validate_input=False)(...), no longer exists in the tree"
  - id: R7
    tag: P2
    owner: vivi
    milestone: "M4,M7"
    risk: "Lock semantics on non-local filesystems"
    mitigation: "flock plus DB writer_lease with TTL/heartbeat; magicite doctor warns; documented in docs/operations.md"
  - id: R8
    tag: P2
    owner: vivi
    milestone: "M6"
    risk: "Distillation quality without an in-server LLM (CR-3)"
    mitigation: "support/consistency thresholds, approval-gated proposals landing nascent+pending; revisit if acceptance < 30%"
  - id: R9
    tag: P2
    owner: vivi
    milestone: "M2"
    risk: "Cold start / small registries (<50 skills Magicite is overhead)"
    mitigation: "route() returns registry_size; magicite doctor states break-even honestly"
  - id: R10
    tag: P1
    owner: kupo+vivi
    milestone: "M6"
    risk: "Hypothesis falsification (H-BODY/H-SCALE/H-COMPOSE/H-LEARN are UNVERIFIED-2026). FIRED 2026-08-15 on 70 engrams / 210 pre-registered queries: H-BODY-a supported in DIRECTION (+14.3pp Hit@1, p=0.00064) but the registered >=20pp effect size is NOT demonstrated; H-BODY-b FALSIFIED as implemented ((b) > (d), -0.0857, p=0.00053); H-SCALE mechanism FALSIFIED at 70 skills with 5 real communities; H-SCALE claim INCONCLUSIVE; H-COMPOSE UNTESTED (zero compositional queries were run and Plan F1 as implemented is a monotone re-encoding of Hit@1); H-LEARN FALSIFIED as implemented under uniform demand. The mitigation worked as designed - cheap and early."
    mitigation: "bench harness with baselines a-d ships in v1 so falsification is early and cheap. A measured falsification is the mitigation SUCCEEDING: what it now requires is that the product claim be reframed to what the evidence licenses - a semantic skill router with a portable format, a lifecycle, governance, composition-plan expansion and an instrumented learning substrate that is NOT yet demonstrated to improve routing. Two routing defaults moved on that evidence (errata DECLARED-EDGES-AMENDED); the docs/01 + docs/07 corrections are routed to IDG as CF-1."
    fired: true
    fired_at: "2026-08-15"
  - id: R11
    tag: P1
    owner: kupo
    milestone: "verify"
    risk: "Critic independence \u2014 the critic pass ran in-session (no second agent available)"
    mitigation: "disclosed, not laundered; Kupo is the independent checker at ESL verify; frozen criteria hash is the tamper anchor"
  - id: R12
    tag: P1
    owner: vivi+kupo
    milestone: "M2,M6"
    added_by: DECLARED-EDGES-AMENDED
    fired: true
    fired_at: "2026-08-15"
    status: "OPEN, NARROWED (errata R12-FIRED)"
    risk: "Newly-live declared-edge mass is itself unmeasured. Section 3.3.1 puts declared composes/depends_on into the activation graph and makes inhibition arithmetically real for the first time - 11 declared inhibits relations in the 70-engram benchmark registry that have NEVER had any effect now scale their targets' activation by 0.3. ppr_restart=0.85 was measured on the OLD graph, so its 0.5476 Hit@1 does not transfer unchanged; community structure re-clusters once _COMMUNITY_WEIGHT_FLOOR is deleted; and baseline (c)'s published numbers move. Shipping on an obsolete measurement is exactly the failure this amendment exists to correct. [FIRED 2026-08-15, errata R12-FIRED - the mitigation worked as designed: the release obligation forced the re-measurement BEFORE release. Outcome, published in spec.md section 3.3.1 at commit 2d25abb: ppr_restart=0.85 is CONFIRMED on the new graph shape (it is what recovers (c) 0.4333 -> 0.5286 and (d) 0.4905 -> 0.5476), so that clause is CLOSED. declared_edge_strength=1.0 measured -0.0286 Hit@1 in (d) - 115 -> 109 of 210, six queries, no paired test run, ceiling p=0.031 - and STAYS AT 1.0, because baseline (c) carries the same declared mass into the diffusion graph without inhibition or the community rerank and is IDENTICAL across both arms (0.5286, 111/210): the diffusion channel measured EXACTLY INERT, so the -6 arises in a channel the run never isolated. WHAT REMAINS UNMEASURED IS THEREFORE NARROWER: the INHIBITION MAGNITUDE - inhib_gain=0.7 predates the amendment, was set for a LEARNED S over [0,1], and now cuts 70% of an inhibited node's activation from one line of author YAML at S=1.0 - and the COMMUNITY RE-CLUSTERING. MO-3 is still owed.]"
    mitigation: "FOUR PRE-REGISTERED REVERSAL CONDITIONS WITH DECISION RULES ATTACHED (spec.md section 3.3.1, RC-1...RC-4), applied without a further judgement call. RC-1 (= MO-3, already owed): isolate inhibition (inhib_gain 0.7 vs 0.0 at declared_edge_strength=1.0) - if it accounts for the whole -0.0286 the defect is the inhibition magnitude, not the authored channel; if none of it, call-site row 3's community re-clustering is what gets revisited. RC-2: a SECOND, INDEPENDENTLY-AUTHORED corpus plus a PAIRED McNEMAR test - at p < 0.05 against, declared_edge_strength ships at 0.0 (mechanism implemented, verified, opt-in) and AC-035 is restated. RC-3 (= CF-7): a compositional query set - H-COMPOSE is still UNTESTED - and a null there retires the diffusion claim regardless of RC-1. RC-4: a known-good declared-relation registry, to remove the single-author confound. The change remains ONE CONFIG SCALAR AND EXACTLY REVERTIBLE - declared_edge_strength=0.0 reproduces pre-amendment scores bit-for-bit (AC-039, whose proving unit pins 0.0 explicitly and is therefore satisfiable at ANY shipped default) - so every reversal is a config line, not a code change. RECORDED AGAINST 0.0 AS A SHIPPED DEFAULT: it makes AC-023 unreachable in production again, makes AC-035's THEN FALSE AS WRITTEN (raw weight 0.0, build_graph drops w<=0, and the proving unit's 'assert raw_weight > 0' fails at the default - so it needs an AC-035 restatement plus a tests/ change), and drops declared edges from community structure entirely, which is WORSE than pre-amendment now that _COMMUNITY_WEIGHT_FLOOR is deleted. NOT sufficient as evidence: more queries on the SAME corpus - the confound is not statistical."

# ── corpus contradictions resolved unilaterally (spec section 9) ───────────
corpus_contradictions:
  - id: CR-1
    confidence: high
    tension: "route() updates exposure_count (docs/05) vs Tier-A file fields being Dream-only (docs/02-04)"
    resolution: "bookkeeping counters are Tier-C accumulators (eph_bookkeeping); the file value is last-checkpoint + delta, folded in at Dream phase 7"
  - id: CR-2
    confidence: high
    tension: "skill-graph.db 'can be committed' (docs/06) vs the DB being a rebuildable cache (docs/03,04)"
    resolution: "path stays per docs/02; register() writes a .gitignore excluding skill-graph.db*; MAGICITE_COMMIT_DB=1 restores the docs/06 reading"
  - id: CR-3
    confidence: high
    tension: "Dream drafts composites 'via local model' (docs/03) and the LLM rubric (docs/07) vs local-first no-service posture (docs/02)"
    resolution: "the v1 server ships no generative model: nucleate emits proposals (trace IR + skeleton), the host agent drafts and calls register(); the rubric gate is a deterministic 12-point structural rubric at the docs/07 >=8/12 bar; rubric_provider=host reserved"
  - id: CR-4
    confidence: high
    tension: "register()'s 'any lint violation -> hard error' vs GAP-005 wholesale SKILL.md migration"
    resolution: "two lint profiles: strict keeps the hard error; import downgrades to warnings and lands status=draft + verification_status=pending, never routable, never silently accepted"
  - id: CR-5
    confidence: high
    tension: "the 16-tool surface (brief, docs/05's 8+8 arithmetic) vs 13 tools actually headed in docs/05"
    resolution: "v1 ships exactly 16 = 14 unified inventory + load_skill_body (docs/05 progressive disclosure) + session_end (docs/03 session-end trace, the on-session-end Dream trigger); 8+8 is pre-deduplication"
  - id: CR-6
    confidence: high
    tension: "Ollama named in docs/02 vs 'local embedding model' plus an offline hardened container"
    resolution: "Embedder protocol with fastembed (default, baked), ollama (opt-in, preserves the docs/02 reading), hashing (deterministic, CI); embedding.model records the producer so docs/04's bge-m3 example stays legal"
  - id: CR-7
    confidence: high
    tension: "docs/06 says imports start status=pending vs docs/04's status=nascent + verification_status=pending"
    resolution: "the two axes stay orthogonal; pending is never a lifecycle status; imports are status in {nascent, draft} x verification_status=pending"
  - id: CR-8
    confidence: high
    tension: "docs/04 defines id as the content-hash of identity+routing while version bumps on every sharpening \u2014 sharpening would change every id and dangle every edge FK"
    resolution: "id is computed once at first registration and is thereafter immutable; the identity+routing hash lives separately as identity_sha256 for drift and duplicate-import detection (found by the critic pass)"

# ── declared execution scope (drift watch; recorded in plan-state.json) ────
declared_scope:
  - "src/magicite/*"
  - "tests/*"
  - "pyproject.toml"
  - "uv.lock"
  - "Dockerfile"
  - "Dockerfile.dev"
  - ".dockerignore"
  - ".gitignore"
  - ".github/workflows/*"
  - "README.md"
  - "LICENSE"
  - "docs/adapters/*"
  - "docs/operations.md"

# ── artifacts (hashes as emitted at Assemble; spec.yaml is a projection) ───
artifacts:
  - path: ".spectra/changes/magicite-v1-implementation/spec.md"
    sha256: 757313fc568c2b2332d67e255f25b493b7f03c16583fb6a8e1693b4e29200440
    role: "normative spec (payload of the ECL envelope); rehashed 2026-08-15 by errata R12-FIRED (was e9efab60…, itself rehashed 2026-08-15 by DECLARED-EDGES-AMENDED from 57148a2d…, itself rehashed 2026-08-15 by R1-RESTATED from 9fca1c08…, itself rehashed 2026-08-14 by A1-REVISED from 92372ad6…). Five links in the chain."
  - path: ".spectra/changes/magicite-v1-implementation/acceptance-criteria.md"
    sha256: 7bd3d1842de8d0d55c00fc4803e52e012885a26094cc928b7801af556eada448
    role: "frozen EARS criteria (ramza-freeze). BYTE-IDENTICAL through all FOUR errata; never re-frozen. This is the tamper anchor — if this hash ever moves without a recorded plan-state amendments[] entry, that is tamper evidence."
  - path: ".spectra/changes/magicite-v1-implementation/acceptance-criteria-addendum.md"
    sha256: 2888886351b9d658ab98bf1d2e94d77e43850201130a15b26be9c63f3e27ef02
    role: "AC-034 … AC-042 (9), added 2026-08-15 by DECLARED-EDGES-AMENDED. NOT frozen and NOT merged into the frozen file; this hash is their tamper anchor. ramza-ears-lint 9/9 pass. BYTE-IDENTICAL through errata R12-FIRED — no criterion was added, edited or removed; AC-039 is satisfiable at any shipped declared_edge_strength because its proving unit pins 0.0 explicitly, and AC-035's dependence on a NON-ZERO default is recorded in that errata as a consequence of the rejected 0.0 option, not acted on."
  - path: ".spectra/changes/magicite-v1-implementation/plan-state.json"
    sha256: abfc21fe9a026d66a0dbc7e594c8709ae71f969f33fabae20385fb24ef481646
    role: "tamper-evident audit trail (ramza/plan-state.v1); rehashed 2026-08-15 — FOURTH errata[] entry appended, pure append verified against HEAD, 0 deletions (was 5d1483b5…; amendments[] still empty, criteria_sha256 untouched)"
  - path: ".spectra/changes/magicite-v1-implementation/spec.envelope.json"
    sha256: a8d3cce827952af9056526cd61178d782b116f5193eaedeb63087fc34527d6f1
    role: "ECL v2.0 PROPOSE envelope -> vivi; integrity tag re-stamped 2026-08-15 over the amended spec.md, FOURTH x_ramza_amendment entry appended so the envelope carries the full chain 92372ad6… -> 9fca1c08… -> 57148a2d… -> e9efab60… -> 757313fc… (was 108704ba…). ramza-verify-emit green."
  - path: ".spectra/changes/magicite-v1-implementation/decisions/A1-REVISED.md"
    sha256: a5801a0c9210f59740f42c082707318ec2e80d122c86ef5ec6d96a1691ea3d39
    role: "append-only decision record for the A1 revision (2026-08-14); untouched by errata R1-RESTATED, hash re-verified on disk"
  - path: ".spectra/changes/magicite-v1-implementation/decisions/R1-RESTATED.md"
    sha256: 2b769225d06414b7d88e173dcc93e2c88aed5048b68f0bf7aeb571105dd96eb7
    role: "append-only decision record for the R1 mitigation restatement (2026-08-15); untouched by errata DECLARED-EDGES-AMENDED, hash re-verified on disk"
  - path: ".spectra/changes/magicite-v1-implementation/decisions/DECLARED-EDGES-AMENDED.md"
    sha256: aec7dd1cb1c5e2aa8cc17951215225cd9850b3198185d46ff768fca66422659e
    role: "append-only decision record for the declared-edge weight amendment and the two evidence-driven routing defaults (2026-08-15); untouched by errata R12-FIRED and NOT superseded by it, hash re-verified on disk"
  - path: ".spectra/changes/magicite-v1-implementation/decisions/R12-FIRED.md"
    sha256: b58302a63a875d31f98a8f5c83e421e36e1a147afb4262529e4703f1ad8b8504
    role: "append-only decision record for R12 firing: the published re-measurement, ppr_restart=0.85 CONFIRMED on the new graph shape, declared_edge_strength CONFIRMED at 1.0, the (c)-invariance channel isolation, the negative product finding on H-BODY-b as designed, and the four pre-registered reversal conditions RC-1…RC-4 (2026-08-15)"
  - path: ".spectra/changes/magicite-v1-implementation/ramza-calibration.jsonl"
    sha256: f2f3ac72015b57689210e55348e9f73c5e7e2ad6ca45df8ece22800c069a61c4
    role: "instrument-vs-outcome calibration log"

# ── handoff ────────────────────────────────────────────────────────────────
handoff:
  to: vivi
  performative: PROPOSE
  message_id: "01a00270-c5bd-74b1-999b-089d1f45df2e"
  verdict: VALIDATE — human validation wanted on the eight CR resolutions (CR-3 and CR-4 change what v1 does) and on assumption A1 before implementation starts
  not_vivi_s_call: [the 16-tool surface, the Tier A/B/C split, the P0 guard mechanism, any section-9 resolution]
