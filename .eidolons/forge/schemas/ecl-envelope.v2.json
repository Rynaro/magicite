{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Rynaro/FORGE/blob/v1.10.0/schemas/ecl-envelope.v2.json",
  "title": "ECL envelope v2",
  "description": "Sidecar envelope written next to every emitted Eidolon artefact. Vendored from eidolons-ecl/schemas/envelope.v2.json with performative enum and context-delta body inlined for standalone validation, following the same convention as the retained schemas/ecl-envelope.v1.json (kept in-tree for ECL §7.3 back-compat validation of inbound v1.x sidecars). ECL v2.0 adds the OPTIONAL ise (Intent, Source, Entitlement) trust-hierarchy block (ECL v2.0 §6.5); everything else is unchanged from v1.0. Defined by ECL v2.0 §1.",
  "type": "object",
  "required": [
    "envelope_version",
    "message_id",
    "thread_id",
    "parent_id",
    "from",
    "to",
    "performative",
    "objective",
    "artifact",
    "integrity",
    "trace"
  ],
  "properties": {
    "envelope_version": {
      "type": "string",
      "pattern": "^(1\\.[012]|2\\.0)(\\.\\d+)?$",
      "description": "ECL spec version this envelope conforms to. ECL v2.0 §1.1.1. Accepts v1.0, v1.1, v1.2 (§7.3 compatibility window, through 2027-05-13) and v2.0."
    },
    "message_id": {
      "type": "string",
      "format": "uuid",
      "description": "Globally unique ID. UUIDv7 RECOMMENDED."
    },
    "thread_id": {
      "type": "string",
      "format": "uuid",
      "description": "UUID grouping all envelopes of one logical mission."
    },
    "parent_id": {
      "oneOf": [
        { "type": "null" },
        { "type": "string", "format": "uuid" }
      ],
      "description": "message_id of the immediate causal predecessor. null only on the first envelope of a thread."
    },
    "from": { "$ref": "#/$defs/agentRef" },
    "to":   { "$ref": "#/$defs/agentRef" },
    "performative": {
      "enum": [
        "REQUEST",
        "INFORM",
        "PROPOSE",
        "CRITIQUE",
        "DECIDE",
        "DELEGATE",
        "ACKNOWLEDGE",
        "ESCALATE",
        "RESUME",
        "REFUSE"
      ],
      "description": "Sender's declared intent. ECL v2.0 §2. Closed ten-performative set, unchanged from v1.0. Inlined from performative.v1.json."
    },
    "edge_origin": {
      "enum": ["roster", "composition", "implicit"],
      "description": "Where the (from, to) edge is declared. ECL v2.0 §3.3."
    },
    "objective": {
      "type": "string",
      "minLength": 1,
      "maxLength": 240,
      "description": "Single sentence stating the goal of this message."
    },
    "artifact": {
      "type": "object",
      "required": ["kind", "schema_version", "path", "sha256", "size_bytes"],
      "properties": {
        "kind": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Slug naming the payload kind (e.g. 'reasoning-report')."
        },
        "schema_version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+(\\.\\d+)?$",
          "description": "SemVer of the per-Eidolon profile schema validating this payload."
        },
        "path": {
          "type": "string",
          "pattern": "^(?!/)(?!.*\\.\\.).+$",
          "description": "Relative path. MUST NOT begin with '/' or contain '..'."
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Lowercase hex digest of the payload bytes at emit time."
        },
        "size_bytes": {
          "type": "integer",
          "minimum": 0,
          "description": "Payload byte count at emit time."
        }
      },
      "additionalProperties": false
    },
    "context_delta": {
      "type": "object",
      "required": ["token_budget", "tokens_used", "input_handles", "summary"],
      "properties": {
        "token_budget": {
          "type": "integer",
          "minimum": 0,
          "description": "Contract-declared maximum token count for this envelope."
        },
        "tokens_used": {
          "type": "integer",
          "minimum": 0,
          "description": "Sender's good-faith estimate of tokens used. MUST be <= token_budget. ECL v2.0 §4.1.1."
        },
        "input_handles": {
          "type": "array",
          "items": {
            "type": "string",
            "anyOf": [
              { "pattern": "^(?!/)(?!.*\\.\\.).+$" },
              { "pattern": "^ecl://thread/[0-9a-f-]{36}/message/[0-9a-f-]{36}$" }
            ]
          },
          "description": "Relative paths or ecl:// URIs the receiver can re-read on demand. ECL v2.0 §4.3."
        },
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "Prose describing what is new since parent_id. SHOULD be <= 200 tokens. ECL v2.0 §4.1.3."
        }
      },
      "additionalProperties": false,
      "description": "Context delta embedded in this envelope. Inlined from context-delta.v1.json. ECL v2.0 §4."
    },
    "constraints": {
      "type": "object",
      "properties": {
        "deadline_ts": {
          "oneOf": [
            { "type": "null" },
            { "type": "string", "format": "date-time" }
          ]
        },
        "trust_level": {
          "enum": ["low", "standard", "high"],
          "default": "standard"
        }
      },
      "additionalProperties": false
    },
    "ise": {
      "$ref": "#/$defs/ise",
      "description": "ISE (Intent, Source, Entitlement) trust-hierarchy block. Composes with, does not replace, constraints.trust_level. OPTIONAL at v2.0 — envelopes without it remain conformant (gate S-1 not-applicable when absent). ECL v2.0 §6.5."
    },
    "expected_response": {
      "type": "object",
      "properties": {
        "performative": {
          "enum": [
            "REQUEST",
            "INFORM",
            "PROPOSE",
            "CRITIQUE",
            "DECIDE",
            "DELEGATE",
            "ACKNOWLEDGE",
            "ESCALATE",
            "RESUME",
            "REFUSE"
          ]
        },
        "shape_hint": {
          "type": "string",
          "description": "Free-form hint about the shape of the expected reply."
        }
      },
      "additionalProperties": false
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Sender's self-assessed confidence in the payload."
    },
    "assumptions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Material assumptions the receiver should know."
    },
    "integrity": {
      "type": "object",
      "required": ["method", "value"],
      "properties": {
        "method": {
          "enum": ["sha256", "hmac-sha256"],
          "description": "ECL v2.0 §6.1."
        },
        "value": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Lowercase hex digest. ECL v2.0 §6.2.1."
        }
      },
      "additionalProperties": false
    },
    "trace": {
      "type": "object",
      "required": ["ts", "host", "model", "tier"],
      "properties": {
        "ts": {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 UTC timestamp at envelope construction."
        },
        "host": {
          "type": "string",
          "description": "Host environment slug (e.g. 'claude-code', 'cursor', 'codex', 'opencode', 'raw')."
        },
        "model": {
          "type": "string",
          "description": "Model identifier (e.g. 'claude-sonnet-4-6'). Free-form to allow vendor strings."
        },
        "tier": {
          "enum": ["standard", "trance"],
          "description": "Routing tier. ECL v2.0 §5.2."
        }
      },
      "additionalProperties": false
    }
  },
  "patternProperties": {
    "^x_[a-z][a-z0-9_]*$": {
      "description": "Vendor extension fields. Receivers SHALL ignore unknown x_* fields. ECL v2.0 §1.2.3."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "agentRef": {
      "type": "object",
      "required": ["eidolon", "version"],
      "properties": {
        "eidolon": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Lowercase slug. Reserved values: 'human', 'orchestrator'."
        },
        "version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$|^n/a$",
          "description": "SemVer of the Eidolon, or the literal 'n/a' for 'human' / 'orchestrator'."
        }
      },
      "additionalProperties": false
    },
    "ise": {
      "type": "object",
      "additionalProperties": false,
      "required": ["assertion_grade"],
      "properties": {
        "assertion_grade": {
          "type": "string",
          "enum": ["unverified", "self-attested", "validated", "human-reviewed"],
          "description": "Emitter's claim about how the artefact was produced (ECL v2.0 §6.5.1 gate S-1). 'unverified' = emitted without internal checks; 'self-attested' = emitter ran its own validation; 'validated' = emitter ran spec-mandated gates; 'human-reviewed' = operator signed off."
        },
        "provenance": {
          "type": "object",
          "additionalProperties": false,
          "required": ["methodology_version"],
          "properties": {
            "methodology_version": {
              "type": "string",
              "pattern": "^[a-z][a-z0-9-]*-\\d+\\.\\d+\\.\\d+$",
              "description": "Eidolon methodology + semver, e.g. 'forge-1.10.0'. ECL v2.0 §6.5.2."
            },
            "tool_surface": {
              "type": "array",
              "items": { "type": "string" },
              "maxItems": 32,
              "description": "Distinct tool primitives invoked during artefact production. FORGE is reasoning-only (P0-1); this is normally empty unless mcp__crystalium__* memory calls were used."
            },
            "lateral_consults": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["eidolon", "performative"],
                "properties": {
                  "eidolon": { "type": "string" },
                  "performative": { "type": "string", "enum": ["REQUEST", "INFORM", "PROPOSE", "CRITIQUE", "DECIDE", "DELEGATE", "ACKNOWLEDGE", "ESCALATE", "RESUME", "REFUSE"] }
                }
              },
              "maxItems": 8,
              "description": "Inline-recorded sibling-Eidolon consults that informed this artefact."
            }
          }
        },
        "receiver_authorization": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "auto_route":  { "type": "boolean", "default": true,  "description": "Receiver MAY hand the envelope to the next contracted edge without operator confirm." },
            "auto_merge":  { "type": "boolean", "default": false, "description": "Receiver MAY merge the artefact into a mainline branch without operator confirm." },
            "auto_deploy": { "type": "boolean", "default": false, "description": "Receiver MAY deploy/publish without operator confirm." }
          },
          "description": "ECL v2.0 §6.5.3. Receivers MUST NOT take an action whose corresponding flag is false (gate S-2)."
        }
      }
    }
  }
}
